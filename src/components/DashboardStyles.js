import { createTheme } from "@mui/material";

const theme = createTheme();

const classes = {
    dashboard: {
        display: "flex",
        minWidth: '100vw',
        minHeight: '100vh',
    },
}

export default classes;